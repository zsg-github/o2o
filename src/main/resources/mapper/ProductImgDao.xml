<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--映射文件要与相对应的mapper接口通过namespace属性进行关联-->
<mapper namespace="com.njupt.o2o.dao.ProductImgDao">

    <!--id为mapper类中方法名-->
    <insert id="batchInsertProductImg" parameterType="java.util.List">
        insert into tb_product_img(img_addr,img_desc,priority,create_time,product_id)
        values
        <foreach collection="list" index="productImg" item="index" separator=",">
            #{productImg.imgAddr},
            #{productImg.imgDesc},
            #{productImg.priority},
            #{productImg.createTime},
            #{productImg.productId}
        </foreach>
    </insert>

</mapper>